// --- TUGAS 01: REDUX PATTERN ---
// Materi: 01-Redux-Pattern.md

// 1. CREATE STORE
// Buat fungsi createStore(reducer) yang punya method:
// - getState(): Ambil state saat ini.
// - dispatch(action): Kirim action untuk ubah state.
// - subscribe(listener): Daftar fungsi yang dipanggil saat state berubah.

function createStore(reducer) {
  // ...
}

// 2. IMPLEMENTASI COUNTER
// Gunakan createStore buatanmu untuk bikin aplikasi Counter sederhana.
