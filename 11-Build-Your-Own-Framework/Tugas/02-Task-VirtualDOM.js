// --- TUGAS 02: VIRTUAL DOM ---
// Materi: 02-Virtual-DOM.md

// 1. H FUNCTION
// Buat fungsi h(tag, props, children) yang mengembalikan VNode object.
// h('div', { id: 'app' }, 'Halo') -> { tag: 'div', props: { id: 'app' }, children: 'Halo' }

function h(tag, props, children) {
  // ...
}

// 2. MOUNT FUNCTION
// Buat fungsi mount(vnode, container) yang mengubah VNode jadi DOM asli dan menempelkannya ke container.
